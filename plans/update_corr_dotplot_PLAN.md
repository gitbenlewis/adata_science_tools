# Plan: Extend `corr_dotplot()` Styling Controls and Resolve `obs`/Feature Collisions

1. Summary: update [`_plotting/_corr_dotplots.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_plotting/_corr_dotplots.py) to add public styling kwargs and deterministic `obs`/feature collision handling, while keeping the current full-matrix assembly path and all statistical behavior unchanged.

2. Public API: add `dot_size: float = 200`, `title_fontsize: int = 20`, `axis_label_fontsize: int = 20`, `tick_label_fontsize: int | None = None`, and `legend_fontsize: int | None = None` to `corr_dotplot()`. Keep `spearman_cor_dotplot()` as a wrapper with no new positional behavior; it should continue forwarding any new keyword args through `**kwargs` and still force `method="spearman"`.

3. Collision handling: in the assembled `adata` / `obs_df` + feature-matrix path only, detect intersections between `_obs_df.columns` and `feature_df.columns` before `pd.concat`, rename the colliding metadata columns to `<name>_obs`, and then concatenate. After this rename, the bare colliding name always refers to the feature column; callers must use `<name>_obs` to target the metadata column in `column_key_x`, `column_key_y`, or `hue`. Do not alter pre-assembled `df` input columns.

4. Styling implementation: replace the hardcoded scatter size `s=200` with `dot_size`; replace the hardcoded `fig.suptitle(..., fontsize=20)` with `title_fontsize`; when a legend is drawn, apply `legend_fontsize` to both legend labels and the legend title; when `tick_label_fontsize` is provided, apply it through `axes.tick_params(axis="both", labelsize=...)`; keep the current label-setting behavior, but when `xlabel` and/or `ylabel` are explicitly provided, use `axis_label_fontsize` instead of the current hardcoded `20`. Do not change regression lines, axes reference lines, palette logic, filtering order, correlation method handling, or return values.

5. Docs and types: update the `corr_dotplot()` docstring to document the new styling kwargs, the collision-renaming rule, and the existing `x_df` behavior as “DataFrame or other 2D matrix-like input convertible to a DataFrame.” Change the `x_df` annotation to `Any | None` so the type hint matches the current runtime behavior, and keep `var_df` documented as the source of column names when `x_df` is not already a `DataFrame`.

6. Tests: add [`tests/test_corr_dotplots.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/tests/test_corr_dotplots.py) in the existing `unittest` style. Cover: an AnnData collision case where an `obs` column and a feature share a name and `hue="<name>_obs"` succeeds without the current duplicate-column failure; styling kwargs propagating to scatter point size, suptitle font size, legend label/title font size, tick label size, and explicit axis label font sizes; `spearman_cor_dotplot()` accepting the new kwargs and still returning a Spearman result. Use `show=False` and close figures during tests.

7. Assumptions and defaults: keep the current dense full-matrix assembly from `adata.X` / `adata.layers[layer]`; keep current numeric handling and SciPy exception behavior untouched; preserve current defaults as closely as possible by using `dot_size=200`, `title_fontsize=20`, `axis_label_fontsize=20` for explicitly set labels, and `None` for `tick_label_fontsize` and `legend_fontsize` so matplotlib defaults remain in effect unless the caller opts in.
