1. **Title**: Add a new regression test module, [`test_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/tests/test_diff_test.py), for `adata_science_tools.diff_test` without modifying [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py).

2. **Summary**: Scope is tests only. The implementation file [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py) must remain unchanged. The plan is to add one new `unittest` module that exercises current behavior through the public API, using deterministic synthetic `AnnData` objects with `Age`, `Gender`, and `Treatment` metadata. If a new test exposes an existing bug in [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py), stop and report it rather than patching that file.

3. **Synthetic Data Design**: In [`test_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/tests/test_diff_test.py), create reusable fixture builders that synthesize `AnnData` with `obs` columns `Age`, `Gender`, and `Treatment`, plus pairing IDs where needed.
   The standard fixture should use 8 rows for 4 subjects, with one `drug` and one `vehicle` row per `SubjectID`, repeated subject-level `Age`/`Gender`, shuffled row order, and features `feat_signal`, `feat_constant`, and `feat_zero`.
   The nested fixture should use 12 rows for 3 animals, with `Treatment` values `drug`, `predoseDrug`, `vehicle`, and `predoseVeh`, repeated `Age`/`Gender`, shuffled row order, `AnimalID` for pairing, and `var["feature_class"]` metadata.
   A non-finite fixture should reuse the same `Age`/`Gender`/`Treatment` structure but inject `np.nan` and `np.inf` into one feature.
   A raw/layer variant should provide different numeric values in `adata.X`, `adata.raw.X`, or a named layer so `use_raw` selection can be tested against observable output changes.

4. **Independent-Test Coverage**: Add one test using the standard synthetic fixture with `groupby_key="Treatment"`, `groupby_key_target_values=["drug"]`, `groupby_key_ref_values=["vehicle"]`, `tests=["ttest_ind", "mannwhitneyu"]`, `add_values2results=True`, and `sortby="Age"`.
   Assert that `feat_zero` is removed from the result index, expected summary/statistic columns exist, the signal feature sorts first by the default statistic ordering, `drug_Age_order` and `vehicle_Age_order` reflect the `Age`-ordered obs values, and `feat_constant` has `ttest_ind_constVAR_target_ref=True` and `mannwhitneyu_constVAR_target_ref=True` with `NaN` stats/p-values.
   Add a second independent test comparing `use_raw=False` and `use_raw=True` on the same synthetic object and assert that the reported `mean:Treatment_drug` and `mean:Treatment_vehicle` values change to the raw-backed values.
   Add a third independent test on the non-finite fixture and assert that means are computed from finite values only, p-values/FDR stay finite when enough finite observations remain, and normality outputs for the contaminated feature become `NaN` rather than raising.

5. **Paired-Test Coverage**: Add one paired test using the standard synthetic fixture with `tests=["ttest_rel", "WilcoxonSigned"]`, `pair_by_key="SubjectID"`, and `add_values2results=True`.
   Assert the exact `SubjectID_order`, exact exported `drug_values`, `vehicle_values`, and `drug_minus_vehicle_values`, presence of paired PCT-change and paired normality columns, and constant-feature `True/NaN` behavior for both paired test families.
   Make at least one exact numeric assertion on the signal feature’s paired-difference export so the alignment logic is locked to `SubjectID` rather than accidental row order.

6. **Nested-Test Coverage**: Add one nested test using the nested synthetic fixture with `groupby_key="Treatment"`, `groupby_key_target_values=["drug"]`, `groupby_key_ref_values=["vehicle"]`, `nested_groupby_key_target_values=[("drug", "predoseDrug")]`, `nested_groupby_key_ref_values=[("vehicle", "predoseVeh")]`, `tests=["ttest_rel_nested", "WilcoxonSigned_nested"]`, `pair_by_key="AnimalID"`, `add_values2results=True`, and `add_adata_var_column_key_list=["feature_class"]`.
   Assert the exact `AnimalID_order`, presence of nested diff-control export columns, merged `feature_class`, default nested sorting, and constant-feature `True/NaN` behavior for both nested statistical branches.

7. **CSV/Input-Coercion and Save Coverage**: Add one tempdir-backed test that writes `x_df`, `obs_df`, and `var_df` CSVs from the synthetic `Age`/`Gender`/`Treatment` fixture, passes them with `adata=None`, and asserts that shuffled `obs_df`/`var_df` inputs are reindexed correctly, var annotations merge onto the result rows, the result CSV is written, and the `.log` sidecar is written when `save_log=True`.
   Keep this test entirely black-box through `adata_science_tools.diff_test`; do not import private helpers from [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py).

8. **`adata.uns` and Error Coverage**: Add one test with `save_result_to_adata_uns_as_dict=True` on the nested synthetic fixture and assert that `adata.uns["diff_test_results"]["Treatment_drug_over_vehicle"]` exists and stores every `_values` and `_order` column as JSON string content rather than Python list objects.
   Add focused `assertRaisesRegex` tests for current guard behavior: missing `groupby_key`, paired/nested calls without `pair_by_key`, partial alternate-input usage, mismatched `obs_df` index, mismatched `var_df` index, missing `save_path` when `save_log=True`, and a disjoint-pair synthetic case where `Treatment` groups have no overlapping `SubjectID` values.

9. **Verification and Constraint Handling**: Run `python -m unittest -v tests.test_diff_test` first, then `python -m unittest discover -s /home/ubuntu/projects/gitbenlewis/adata_science_tools/tests -v`. Accept the work only if the new tests pass in isolation, the full suite remains green, and [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py) is untouched. Default assumption: this is a regression suite for current behavior only, and any defect uncovered in [`_diff_test.py`](/home/ubuntu/projects/gitbenlewis/adata_science_tools/_tools/_diff_test.py) will be reported without modifying that file.
